#list of needed data_tools
import torch
import datetime
import torch.nn as nn
from sklearn import classification_report
import pandas as pd


class data_utils:
    def ner_analysis(self, data : pd.DataFrame) -> dict:

    def Embeddings(self, nn.Embeddings):

    def perform_anlaysis(self, data : pd.DataFrame) -> dict: